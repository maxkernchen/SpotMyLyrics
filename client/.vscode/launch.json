{
    "configurations": [
    {
      "type": "firefox",
      "request": "launch",
      "name": "Launch My Firefox",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "timeout": 90000,
      "tmpDir": "~/Documents/temp_firefox_debug/",
      "pathMappings": [{
        "url": "http://localhost",
        "path": "${workspaceFolder}"
    }]
}
    ]
}

